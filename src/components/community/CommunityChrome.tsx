"use client";

/**
 * Mount point for community UI (sidebars, banners, overlays, etc.).
 * Agents: import your new components here and render them — creating a file alone is not enough.
 */
export function CommunityChrome() {
  return null;
}
